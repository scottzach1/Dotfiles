# hyprpaper — replaces nitrogen
# Wallpapers live under ~/.local/share/backgrounds (shipped under configs/home).
wallpaper {
    monitor =
    path = ~/.local/share/backgrounds/macos-big-sur-light.jpg
    fit_mode = cover
}

splash = false
ipc = on
